import { Injectable } from '@nestjs/common';

@Injectable()
export class MailService {
  //TODO: As the project grows, if email functionality expands (welcome emails, newsletters, verification, notifications), we can later promote mail into its own top-level module.
}
